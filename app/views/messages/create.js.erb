$('.chat-content').html('<%= escape_javascript(render partial: "messages/chat") %>');

document.getElementById("new_message").reset();

var element = document.getElementById("ui-segment");
element.scrollTop = element.scrollHeight;
